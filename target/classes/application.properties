# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Spring Application Configuration
spring.application.name=distributed-file-system

# Database
spring.datasource.url=jdbc:postgresql://ca8lne8pi75f88.cluster-czrs8kj4isg7.us-east-1.rds.amazonaws.com:5432/d3n2m0f3khfmqp
spring.datasource.username=u31dr9lp799cef
spring.datasource.password=p4c58e56ad5361ff8dec0ee263d26e4c2e57766402800935c40f6e1fb07446cde
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect



spring.shell.interactive.enabled=false
spring.autoconfigure.exclude=org.springframework.shell.boot.ApplicationRunnerAutoConfiguration

# Multipart Configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
spring.servlet.multipart.enabled=true

# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=*
spring.web.cors.allowed-headers=*

# DFS Custom Configuration
dfs.storage.path=${DFS_STORAGE_PATH:./dfs_storage}
dfs.server.host=${DFS_SERVER_HOST:localhost}
dfs.server.port=${DFS_SERVER_PORT:8888}
dfs.replication.factor=${DFS_REPLICATION_FACTOR:3}

# Logging Configuration
logging.level.org.pr.dfs=DEBUG
logging.level.org.springframework.web=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=logs/dfs-api.log

# Management Endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# SpringDoc Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method